[project]
name = "whatsapp-twin"
version = "0.1.0"
description = "FastAPI backend for WhatsApp AI Persona Clone"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "httpx>=0.27.0",
]